/*#pragma once
#include "Medicament.h"
#include <exception>
using namespace std;

class ValidatorMed
{
private:
	int err;
	string mesaj;
public:
	ValidatorMed();
	int validate(Medicament);
	string errorMessage();
	~ValidatorMed();
};*/
